# MidnightBSD 4.0.1

# start w /  MidnightBSD 4.0.1--amd64-mini-memstick.img

default install using UFS default sizes

# pkg install xorg-minimal drm-510-kmod libXft xrdb xset xsetroot setxkbmap xmodmap doas
pkgconf

# echo permit persist :wheel > /usr/local/etc/doas.conf
# pw usermod user -G wheel,video or use installer at user create
logout

****
as user
$ doas mport install palemoon geany mc yt-dlp mpg123 gh liberation-fonts-ttf
scrot freecolor fastfetch bash

$ mkdir ~/Source & cd Source
$ git clone https://github.com/ChefIronBelly/MidnightBSD.git
$ cp user files and root files from /boot /etc /home to system

$ cd Source /dwm /dmenu /st and doas make clean install
$ doas mkdir /mnt/usb

# 260115
